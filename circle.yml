machine:
  node:
    version: 8.2.0

dependencies:
  pre:
    - npm install -g npm@5.6.0
    - npm install -g polymer-cli
    - npm install -g bower
    - npm i && bower i
test:
  override:
    - polymer test